# =============================================================================
# Spark Configuration Defaults
# YBIGTA Spark & Flink Homework
# =============================================================================

# 아래 값은 건드리지 마세요
spark.master                    spark://spark-master:7077

# -----------------------------------------------------------------------------
# TODO: Shuffle Partition
#
# groupBy/join을 하면 Spark는 데이터를 파티션 단위로 쪼개서 처리합니다.
# 지금 200으로 설정되어 있는데, 이 클러스터에서 돌리면 시나리오 04가 실패합니다.
# 왜 그런지 생각해보고 적절한 값으로 바꿔주세요.
#
# spark.sql.shuffle.partitions  : groupBy/join 파티션 수 (기본값 200)
# spark.default.parallelism     : RDD 기본 병렬성
# -----------------------------------------------------------------------------
spark.sql.shuffle.partitions    200
# spark.default.parallelism

# 선택: executor 메모리/코어 조정 (docker-compose.student.yml이랑 맞춰야 합니다)
# spark.executor.memory
# spark.executor.cores
