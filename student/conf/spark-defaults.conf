# =============================================================================
# Spark Configuration Defaults
# YBIGTA Spark & Flink Homework
# =============================================================================

# -----------------------------------------------------------------------------
# FIXED SETTINGS - DO NOT MODIFY
# -----------------------------------------------------------------------------
spark.master                    spark://spark-master:7077

# -----------------------------------------------------------------------------
# TODO: Shuffle Partition — 시나리오 04의 핵심입니다.
#
# Spark의 groupBy/join은 내부적으로 데이터를 파티션 단위로 셔플합니다.
# 기본값(200)은 수백 대 클러스터 기준 — 이 환경에서 실행하면 시나리오 04가
# 60초를 훌쩍 넘겨 실패합니다. 왜 그런지 직접 확인해보세요.
#
# spark.sql.shuffle.partitions  : groupBy/join 시 생성할 파티션 수 (기본값 200)
# spark.default.parallelism     : RDD 연산의 기본 병렬성
# -----------------------------------------------------------------------------
spark.sql.shuffle.partitions    200
# spark.default.parallelism

# -----------------------------------------------------------------------------
# 선택: Executor 리소스
# spark.executor.memory와 docker-compose.student.yml의 worker 메모리 제한이
# 맞지 않으면 executor가 뜨지 않습니다.
# -----------------------------------------------------------------------------
# spark.executor.memory
# spark.executor.cores
